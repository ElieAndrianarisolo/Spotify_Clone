// Interface for toast notification information
export interface ToastInfo {
  // The main text content of the toast
  body: string;
  // CSS class name for styling the toast
  className: string;
}
